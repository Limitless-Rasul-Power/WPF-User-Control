<Window x:Class="WpfApp5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp5" xmlns:map="clr-namespace:Microsoft.Maps.MapControl.WPF;assembly=Microsoft.Maps.MapControl.WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <!--<map:Map CredentialsProvider="{Binding Provider}" Mode="Aerial">
            
        </map:Map>-->


        <!--<StackPanel>

            <local:PlaceholderTextBox AcceptReturn="False" PlaceholderText="Username"></local:PlaceholderTextBox>
            <local:PlaceholderTextBox AcceptReturn="True" PlaceholderText="Password"></local:PlaceholderTextBox>

        </StackPanel>-->


        <local:Rating Grid.Row="1"></local:Rating>

        <WrapPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock FontSize="30">Rating:</TextBlock>
            <!--<TextBlock Text="{Binding }"></TextBlock>-->
        </WrapPanel>

    </Grid>
</Window>
